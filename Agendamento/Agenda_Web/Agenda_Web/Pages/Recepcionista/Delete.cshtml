@page
@model Agenda_Web.Pages.Recepcionista.DeleteModel

<h1>Excluir Recepcionista</h1>

@* <div class="container mt-5">
    <p>Tem certeza de que deseja excluir o recepcionista abaixo?</p>

    <dl class="row">
        <dt class="col-sm-2">ID:</dt>
        <dd class="col-sm-10">@Model.Recepcionista.IdRecepcionista</dd>

        <dt class="col-sm-2">Nome:</dt>
        <dd class="col-sm-10">@Model.Recepcionista.Nome</dd>

        <dt class="col-sm-2">Sobrenome:</dt>
        <dd class="col-sm-10">@Model.Recepcionista.Sobrenome</dd>

        <dt class="col-sm-2">Número de Identificação:</dt>
        <dd class="col-sm-10">@Model.Recepcionista.NúmeroIdentificação</dd>

        <dt class="col-sm-2">Número de Telefone:</dt>
        <dd class="col-sm-10">@Model.Recepcionista.NúmeroTelefone</dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Recepcionista.IdRecepcionista" />
        <button type="submit" class="btn btn-danger">Confirmar Exclusão</button>
        <a class="btn btn-secondary" href="/Recepcionista/Index">Cancelar</a>
    </form>
</div> *@

